# IntelliVision - Smart Computer Vision Analyzer
Interactive web application built with OpenCV and Streamlit.
    
## Installation & Running
1. `pip install -r requirements.txt`
2. `streamlit run app.py` or `python -m streamlit run app.py`

## Testing
Run unit tests via PyTest: `pytest tests/` or `python -m pytest tests/`
